{
  "name": "root-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present && poetry run ruff check . && poetry run mypy .",
    "test": "npm run test --workspaces --if-present && poetry run pytest"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
} 